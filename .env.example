# Backend API base URL (no trailing slash)
VITE_API_BASE_URL=https://nodejs-instant-api-production.up.railway.app/api

# Mobile app public URL used in QR codes (no trailing slash)
# This should point to wherever the Ionic app is hosted (e.g. https://instant.app)
VITE_APP_URL=
